# Две башни, рекомендующие фильмы

## Савченко Юрий Дмитриевич

# Постановка задачи

Построение модели, способной вернуть топ-K наиболее релевантных фильмов пользователю на основе его истории взаимодействий (оценок). Важно упомянуть, что моё решение нацелено на retrieval-сценарии, то есть на быстрый отбор сравнительно небольшого числа кандитатов для последующей обработки более мощными ранжировщиками. 

## Формат входных и выходных данных

Понимаю этот раздел в контексте инференса модели  
Вход: 

* идентификатор пользователя типа int  
* K \- количество наиболее релевантных фильмов, ожидаемое на выходе, типа int

Выход: 

* список троек \[идентификатор фильма (int), таргет (float), название фильма(String)\] длины K

## Метрики

Нас интересует попадает ли релевантный фильм в шортлист, поэтому метрики следующие:

1. **Recall@K** \- доля случаев, когда размеченный истинно релевантным фильм попал в топ-K   
   $Recall@K \= \frac{1}{N}\sum_{i=1}^{N}I\[rank_i\<K\]$  
     
2. **NDCG@K** \- польза от выдачи кандидатов модели, по сути, тот же **Recall@K,** но учитывающий позицию истинно релевантного фильма в топе  
   $NDCG@K \= \frac{1}{N}\sum_{i=1}^{N}I\[rank_i\<K\]\frac{1}{log_2(rank_i+2)}$

Конечно же, эти метрики сильно зависят от K. В реальных современных retrieval моделях обычно K лежит где\-то между 2000-3000 (например, в Яндексе; ссылок не будет \- мб NDA😉), но до таких масштабов выдачу модели я доводить не хочу. Поэтому, например, **Recall@50** \>= 0.25 будет вполне хорошим результатом, ведь, имея только историю оценок пользователя, хотя бы каждый четвертый раз попадать в крохотную пятидесятку звучит круто

##   Валидация и тест

Буду использовать временной сплит, а именно: 

* сортирую историю пользователей по timestamp’у  
* отсекаю самые новые оценки на валидацию  
* каждая валидационная пара помечается истинно релевантной при оценке 4+/5  
* считаются скоры-таргеты для пары \[пользователь, фильм\] по всем фильмам  
* на основе скоров-таргетов вычисляются ранги: $rank_u= |$ { $f: s(u;f) \> s(u;f^+)$ } $|$
* на основе рангов вычисляются метрики и их усреднения по всем юзерам

Таким образом симулируется поведение модели в инференсе: на основе истории оценок предсказать интерес пользователя в новому фильму  
Детерминированность обеспечивается именно тем, что разбиение определятся фиксированными timestamp’ами

## Датасеты

MovieLens 20M \- реальный датасет с рейтингами фильмов от пользователей:

* 20 миллионов оценок  
* 138 тысяч пользователей  
* 27 тысяч фильмов  
* весит 190 мегабайт  
* из важного для моей модели там ещё информация о времени оценки и о названии фильма  
* ссылка: [https://grouplens.org/datasets/movielens/20m/](https://grouplens.org/datasets/movielens/20m/)  
* нам про него даже рассказывали на втором курсе

Собственно из реальности этих данных и вытекают из особенности:

* дана оценка из множества {1, 2, 3, 4, 5}, но хочу выделять только положительный и негативный итог взаимодействия пользователя с фильмом \-\> нужно выбрать порог (по дефолту 4\)  
* проблема холодного старта: множество пользователей (и фильмов) с мизерным числов оценок \-\> выфильтровываем их еще одним нижним порогом (по дефолту 5\)  
* часть фильмов ощутимо популярнее, надо бы это и не недоучесть, и не переучесть \-\> важно иметь бэйзлайн основанный на популярности (см. ниже)

# Моделирование

## Бейзлайн

Выдаёт топ-K самых популярных (разумеется, по количеству положительных оценок) фильмов из трэйна

## Основная модель

*Чем вдохновился*:   
лекцией Воронцова по рекомендательным система из закрытого плэйлиста на ютубе (извиняюсь, тут опять без ссылки, но вот на всякий случай такая же идея для другой задачи: [https://arxiv.org/abs/2004.04906](https://arxiv.org/abs/2004.04906))

*Устройство модели*:   
пользовательская башня отвечает за обучаемый эмбэддинг пользователя (размерность по дефолту 64\) \- $E_u$
документная башня отвечает за обучаемый эмбэддинг фильма (размерность по дефолту 64\) \- $E_f$  
скор-таргет: $s(u;f)=dot(E_u, E_f)$ \- скалярное произведение эмбеддингов

*Процесс обучения*:  
идейно напоминает эвал стадию:

* оценки из множества {1, 2, 3, 4, 5} делятся на положительные и негативные взаимодействия по правилу  $I\[rating \geq threshold\]$ (по дефолту 4\)  
    
* из множества положительных пар \[пользователь, фильм\] выфильтровываются пары где либо пользователь оценил положительно не более $min$ $user$ $interactions$ фильмов, либо фильм был оценён положительно не более чем $min$ $item$ $interactions$ раз (по дефолту и то, и то 5). это нужно чтобы уменьшить проблему холодного старта  
    
* в каждую эпоху идёт максимум $max$ $interactions$ (по дефолту 2 миллиона)  
  положительных пар \[пользователь, фильм\]. это нужно чтобы мой ноутбук не умер  
    
* для каждой положительной пары \[пользователь, фильм\] выбираются пары \[тот же пользователь, другой фильм\] по всем другим фильмам. эти пары помечаются негативными (**in-batch negatives**)  
* для этого множества пар вычисляются скоры-таргеты с учётом температуры $T$ (по дефолту 1): $s(u;f)=\frac{dot(E_u, E_f)}{T}$  
* эти скоры-таргеты поступают в функцию потери \- кросс энтропию

# Внедрение

1\) Формат использования: онлайн-сервис \+ офлайн-индексация

* офлайн этап: посчитать и сохранить эмбеддинги фильмов, сохранить и артефакты (веса модели)  
* онлайн этап: модель упаковывается в формат **MLflow pyfunc** и поднимается HTTP-сервис, который уже принимает/возвращает данные на инференсе как это писано в самом верху файла

2\) Формат использования: локальный python-пакет

* данные скачиваются из публичного источника с помощью **DVC**  
* поднимается **MLflow Tracking** сервер на `127.0.0.1:8080` для логирования экспериментов
